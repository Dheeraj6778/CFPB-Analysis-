version: 2

models:
  - name: dim_date
    columns:
      - name: date_key
        tests: [not_null, unique]
      - name: date
        tests: [not_null, unique]
  - name: dim_company
    columns:
      - name: company_key
        tests: [not_null, unique]
      - name: company
        tests: [not_null]
  - name: dim_product
    columns:
      - name: product_key
        tests: [not_null, unique]
      - name: product
        tests: [not_null]
  - name: dim_state
    columns:
      - name: state_key
        tests: [not_null, unique]
      - name: state
        tests: [not_null,unique]